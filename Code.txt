   sudo mkdir -p /jfrog/artifactory-filestore
   sudo rsync -avh /var/opt/jfrog/artifactory/data/artifactory/filestore/ /jfrog/artifactory-filestore/
   sudo chown -R artifactory:artifactory /jfrog/artifactory-filestore
   sudo systemctl start artifactory
   sudo journalctl -u artifactory -f



sudo pkill -f artifactory
sleep 5
ps aux | grep -i artifactory

